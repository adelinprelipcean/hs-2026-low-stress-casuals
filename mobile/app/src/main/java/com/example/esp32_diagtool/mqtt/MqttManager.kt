package com.example.esp32_diagtool.mqtt

// This class is deprecated and replaced by SocketManager.
// MQTT implementations have been removed as per requirements.
class MqttManager {
    // Empty implementation to avoid build errors if referenced elsewhere (though it shouldn't be)
}
